/**
 * add - adds two integers
 * @a: Character input
 * @b: Character input
 * Return: adds
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
